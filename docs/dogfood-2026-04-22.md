# Dogfood pass — 2026-04-22

First end-to-end run of Efterlev against a real Terraform codebase —
the govnotes-demo synthetic boundary — rather than the hand-crafted
fixture the E2E smoke uses. govnotes-demo carries a `DELIBERATE_GAPS.md`
with 12 ground-truth compliance gaps, so this pass doubles as a
coverage-accuracy measurement against a known baseline.

## Run parameters

- **Target:** `govnotes-demo/infra/terraform/` only (staging env is
  explicitly out of scope per `DELIBERATE_GAPS.md`). 13 `.tf` files,
  84 parsed resources.
- **Efterlev branch:** `main` at `a68ec7d` (12 detectors, post-Phase-6-lite).
- **Pipeline:** `init → scan → agent gap → agent document → agent remediate --ksi KSI-SVC-VRI` via `uv run efterlev …`.
- **Artifacts:** under `.dogfood/20260423T002344Z/` (gitignored).

## Detector hit-rate against the 12 ground-truth gaps

| # | Gap | Efterlev | Detector |
|---|---|---|---|
| 1 | `user_uploads` bucket no encryption | **missed** | parser (module blind spot) |
| 2 | `user_uploads` bucket no versioning | **missed** | parser (module blind spot) |
| 3 | `bastion_scratch` EBS unencrypted | **missed** | no detector |
| 4 | `legacy_api` weak TLS policy | **caught** | `fips_ssl_policies_on_lb_listeners` |
| 5 | ALB HTTP without HTTPS redirect | **partial** | `tls_on_lb_listeners` sees the HTTP listener; redirect check absent |
| 6 | KMS `assets` rotation off | **caught** | `kms_key_rotation` |
| 7 | `readonly_auditor` IAM policy no MFA | **missed** | `mfa_required_on_iam_policies` — policy uses `jsonencode(data.aws_iam_policy_document…)` and returns `mfa_required=unparseable` |
| 8 | `ci_deploy` long-lived IAM user with access keys | **missed** | no detector |
| 9 | `analytics-db` RDS retention = 1 day | **caught** | `backup_retention_configured` |
| 10 | KMS `reports` overly permissive key policy | **missed** | no detector |
| 11 | `data_ops` IAM role MFA only on destructive | **missed** | `mfa_required_on_iam_policies` (same `jsonencode` path as #7) |
| 12 | CloudTrail missing log-file validation + data events | **partial** | `cloudtrail_log_file_validation` caught; `cloudtrail_audit_logging` doesn't flag missing data-event selectors |

**Breakdown:** 3 fully caught, 2 partially caught, 2 parser-blind, 2
missing-detector, 3 detector-limitation (jsonencode ×2, CloudTrail
data events ×1). Coverage is 3/12 = 25% full-catch; counting partials
generously takes it to 5/12 ≈ 40%.

## Gap Agent reasoning (where detectors fire)

Quality is good on evidence it actually receives:

| KSI | Classification | Ground-truth expectation | Assessment |
|---|---|---|---|
| KSI-SVC-VRI | partial | partial-shaped (#4, #6, #10) | correct — cites the two FIPS listener evidences, explicitly notes what's NOT covered (AMI signing, container signing, IaC signing, backups) |
| KSI-SVC-SNT | partial | partial (#4, #5) | correct — cites HTTP-listener + legacy-api + working-primary |
| KSI-IAM-MFA | partial | partial (#7, #8, #11) | correct and honest — rationale explicitly names the unparseable-policies limitation |
| KSI-MLA-OSM | partial | partial (#12) | correct — cites log-file-validation disabled |
| KSI-MLA-LET | **implemented** | partial per ground truth (missing data events) | defensible reading — KSI statement is outcome-shaped ("maintain a list… log them"); CloudTrail + flow logs meet it if you don't require exhaustive data-event coverage. Calibration difference, not an error |
| KSI-RPL-ABO | partial | partial (#9) | correct — cites 1-day retention |

**Finding:** the Gap Agent reasons well when it gets evidence. The
bottleneck is coverage, not reasoning.

## Doc Agent narratives

Spot-checked KSI-SVC-VRI: 1538-char narrative, correctly
evidence-grounded (cites two sha256 IDs with source paths + line
ranges), and explicitly names the procedural / image-signing
territory that the detectors don't cover. This is the
drafts-not-authorizations discipline working as advertised.

Citations come from the scanner evidence the Gap Agent cited, not
fabricated — the post-generation fence validator held on a real
model run against substantive Terraform.

## Remediation Agent

Ran against `--ksi KSI-SVC-VRI`. Produced a clean unified diff
upgrading `legacy_api`'s `ssl_policy` from
`ELBSecurityPolicy-2016-08` to `ELBSecurityPolicy-TLS13-1-2-2021-06`,
matching the primary listener. The explanation named the
operational-cutover caveat (the code comment about legacy
government-customer integrations), offered a TLS-1.2 fallback policy
for strict-1.3 concerns, and named the dimensions it doesn't cover
(AMI signing, container signing, IaC artifact integrity, backup
integrity) — each is a separate remediation outside this file.

**But:** the picked gap is #4 (legacy_api TLS), not the ground-truth
showcase #1 (`user_uploads` encryption). Root cause: `user_uploads`
evidence never reached the Remediation Agent because the parser
didn't see the bucket (see finding #1 below).

## Findings ranked by leverage

### 1. Terraform module `for_each` blind spot (CRITICAL)

`govnotes` declares 5 buckets through a storage module with
`for_each = var.buckets` over a map. Our HCL parser sees ONE
`aws_s3_bucket "this"` resource declaration (inside the module) and
cannot resolve it into per-key buckets without executing Terraform.
Concretely, `scan_terraform` found 2 `aws_s3_bucket` resources total
when the applied state contains 5+. Ground-truth gaps #1 and #2 (both
on the `user_uploads` map entry) are invisible as a direct
consequence, and the Remediation Agent couldn't surface the showcase
gap for the same reason.

**Fix path:** Terraform Plan JSON support is already named in the v1
locked plan (`docs/dual_horizon_plan.md` §3.1 Month 1: "Source
expansion: Terraform Plan JSON support (scans resolved plans
including computed values)"). Pulling that forward is the single
highest-leverage follow-up after this dogfood — module-heavy
codebases are the common case, not the edge case, and we're
underreporting by ~60% on buckets in this target.

### 2. `jsonencode(data.aws_iam_policy_document…)` limits MFA detection (HIGH)

All 7 IAM policies in govnotes declare their policy body via
`jsonencode(data.aws_iam_policy_document…)` rather than heredoc
literal JSON. Our `mfa_required_on_iam_policies` detector honestly
emits `mfa_required=unparseable` with a clear gap string — the
docstring anticipates this — but it means we cannot detect MFA
enforcement at all on any of the real-world policies. Ground-truth
gaps #7 and #11 (and the "present-correctly" control for platform
admin) all land in the unparseable bucket. The Gap Agent honestly
surfaces this in its KSI-IAM-MFA rationale, but the customer value
of the detector collapses on this pattern.

**Fix paths, ranked:**
1. **Parse `data "aws_iam_policy_document"` blocks statically.** HCL
   declares the statement structure inline (Effect, Action, Resource,
   Condition) — Terraform evaluates it to JSON, but the static
   structure is observable from the HCL AST. Walk the
   `aws_iam_policy_document` data blocks, find those referenced by
   `jsonencode(data.aws_iam_policy_document.X.json)` in `aws_iam_policy.policy`,
   and check their `condition` blocks for `aws:MultiFactorAuthPresent`.
   Moderate effort, big payoff.
2. **Terraform Plan JSON support.** Once plan-JSON is in, this
   collapses — the resolved policy body is a plain JSON string the
   existing detector already handles.
3. **Accept the limitation and cover via Manifest.** Customer
   attests: "our IAM policies require MFA per our policy review
   process." Weakens the Evidence-Manifest distinction we've been
   deliberate about.

Option 1 is the right v1 investment; option 2 subsumes it in v1.5+.

### 3. Missing detectors matching 3 ground-truth gaps (MEDIUM)

| Missing detector | Gap | Rough shape |
|---|---|---|
| `aws.encryption_ebs` | #3 (bastion_scratch unencrypted) | `aws_ebs_volume.encrypted` + `kms_key_id` — parallels `rds_encryption_at_rest` |
| `aws.iam_user_access_keys` | #8 (ci_deploy long-lived keys) | flags any `aws_iam_access_key` resource; optionally cross-references `aws_iam_user` + whether a federation alternative exists |
| `aws.kms_key_policy` | #10 (reports wildcard principal) | parses `aws_kms_key.policy` / `data.aws_iam_policy_document` for `Principal: "*"` patterns |

All three fit the established detector-contract shape. Each is
~0.5–1 day and is a natural Phase 6-full candidate.

### 4. Detector enhancements (MEDIUM)

- **`tls_on_lb_listeners`** sees HTTP listeners on port 80 but does
  not check whether the `default_action` is a `redirect` to HTTPS
  (the mitigating control for having port 80 open at all). Ground
  truth #5 would upgrade from "HTTP listener detected" to "HTTP
  listener without redirect" with this enhancement.
- **`cloudtrail_audit_logging`** checks for presence of
  `event_selector` blocks but not whether any of them declare
  `data_resource` entries for S3 / Lambda. Ground truth #12's
  data-events half would be caught by adding a second evidence
  field like `has_data_event_selectors: bool`.

Both are small changes to existing detectors, not new folders.

### 5. `ksis=[]` detectors' evidence is invisible to KSI-level reasoning (LOW-MEDIUM)

The honest `ksis=[]` posture (5 of 12 detectors) means their
evidence lands in the "unmapped" bucket in the Gap Agent prompt.
The Gap Agent can reach into that bucket but doesn't by default —
the KSI prompt leads with mapped evidence. Concretely, KSI-SVC-VRI
was classified citing only `fips_ssl_policies` evidence (which has
an explicit KSI claim), not the S3 / RDS / KMS evidence which all
touch "cryptographic integrity of resources" (KSI-SVC-VRI's stated
outcome).

**This is not wrong** — the discipline in DECISIONS 2026-04-21
design call #1 is deliberately conservative, and per-control-to-KSI
expansion is exactly the kind of stretching we explicitly chose not
to do. But when a future FRMR release adds SC-28 / SC-12 to a KSI's
`controls` array, the existing detectors should pick up the mapping
without a code change. Worth verifying the Gap prompt handles that
transition when it happens; not blocking now.

### 6. UX: scan output "Detector record IDs" list is noisy (LOW)

The final section of `efterlev scan` lists one line per evidence
record in the form `<sha256>  <resource_name>`. On govnotes, 11 of
31 records have a `resource_name` that appears on another record
too (e.g., `cloudtrail` = the trail, the bucket, the SSE resource,
and the backup retention record). A reader can't tell which detector
produced which without walking the provenance graph. Small
improvement: include the short detector id on each line.

## What this does NOT measure

- **Real FedRAMP-Moderate coverage ceiling.** We've covered 12
  detectors; the target is 30. Even at 30, scanner-only coverage
  tops out around the ~20% line per `docs/dual_horizon_plan.md`
  §3.1 — the remaining 60% is procedural (Evidence Manifests).
  govnotes-demo is synthetic SaaS infra; a real customer's posture
  depends heavily on whether they've authored matching manifests.
- **Plan-vs-code semantic accuracy.** We're reading `.tf` source,
  not `terraform plan -json`. A bucket that's provisioned via a
  local-source module with dynamic values (the `for_each` case) is
  invisible to us; one provisioned via plan JSON would be visible.
  Until Plan JSON lands, coverage on module-heavy codebases (most
  of them) is structurally understated.
- **Cost / token-count baseline.** First-run numbers only: Gap ~103s,
  Doc ~417s, Remediation ~21s. Real CI integration wants per-stage
  budget caps; out of scope here.

## Concrete follow-up items (sized)

| Priority | Item | Rough effort |
|---|---|---|
| P0 | Pull Terraform Plan JSON support forward from v1 Month 1 source-expansion | ~1 week |
| P1 | Static parsing of `data "aws_iam_policy_document"` in `mfa_required_on_iam_policies` | ~2–3 days |
| P1 | New detector: `aws.encryption_ebs` | ~0.5 day |
| P1 | New detector: `aws.iam_user_access_keys` | ~1 day |
| P2 | New detector: `aws.kms_key_policy` (wildcard-principal) | ~1–2 days |
| P2 | Enhance `tls_on_lb_listeners` to check HTTP→HTTPS redirect | ~0.5 day |
| P2 | Enhance `cloudtrail_audit_logging` to detect data-event selectors | ~0.5 day |
| P3 | `efterlev scan` record-list display: include detector id per line | ~15 minutes |

P0 is the single biggest move. P1 cluster would double detector
accuracy on real codebases. P2s are incremental. P3 is an immediate
quality-of-life fix worth bundling with the next commit.

## Measured lift — two P1 detectors added after this report (2026-04-22)

Two detectors from the P1 cluster landed on branch `coverage-followup`
immediately after this report was written:

- **`aws.encryption_ebs`** — scans `aws_ebs_volume.encrypted` AND
  `aws_instance.{root_block_device, ebs_block_device}[*].encrypted`.
  Emits per-block Evidence so mixed postures (encrypted root +
  unencrypted data volume on the same host) are visible.
- **`aws.iam_user_access_keys`** — flags every declared
  `aws_iam_access_key` (Active or Inactive) as a posture gap. Claims
  `KSI-IAM-MFA` per the discipline in the Phase 6-lite DECISIONS
  entry (access keys bypass MFA by design, which is material to the
  KSI's statement).

Re-scanning govnotes (`.dogfood/20260423T011957Z/`) confirms:

- Gap #3 (`bastion_scratch` EBS unencrypted): now caught.
  `encryption_ebs` emits `bastion_scratch` absent **and**
  `bastion.root_block_device` present — the Gap Agent sees the
  mixed-posture evidence on the same host.
- Gap #8 (`ci_deploy` long-lived key): now caught.
  `iam_user_access_keys` records attached user + Active status +
  gap string pointing at federated-identity migration.

**Updated hit-rate:** 5 fully caught (#3, #4, #6, #8, #9) + 2 partially
caught (#5, #12) = 5/12 ≈ 42% full-catch, 7/12 ≈ 58% counting
partials. Up from 3/12 and 5/12 respectively.

Remaining un-caught ground-truth gaps: #1, #2 (parser — needs Plan
JSON support), #7, #11 (IAM `jsonencode` pattern — needs
`data "aws_iam_policy_document"` static parsing), #10 (no detector
— `aws.kms_key_policy` is a known follow-up), and the data-events
half of #12 (`cloudtrail_audit_logging` enhancement).

## Plan-JSON re-run — second measured lift (2026-04-22)

Second lift after `scan_terraform_plan` primitive + `efterlev scan --plan FILE`
landed on main. Re-scanned govnotes by generating a plan with Terraform 1.14
(`terraform plan -refresh=false -out=X && terraform show -json X`) and running
`efterlev scan --target . --plan govnotes.plan.json`. Results in
`.dogfood/<ts>-plan/`.

**Scan delta HCL-mode vs plan-mode (govnotes-demo boundary):**

| Metric | HCL | Plan |
|---|---|---|
| Resources parsed | 84 | 114 |
| Evidence records | 34 | 48 |
| `aws_s3_bucket` seen | 2 | 6 |
| `aws.encryption_s3_at_rest` records | 4 | 11 |
| `aws.s3_public_access_block` records | 2 | 6 |
| `aws.backup_retention_configured` records | 4 | 7 |

The ground-truth deltas that matter:

| # | Gap | HCL | Plan | Comment |
|---|---|---|---|---|
| 1 | user_uploads no encryption | missed | **caught** | "bucket declared without inline server_side_encryption_configuration" — exactly the showcase finding from DELIBERATE_GAPS.md |
| 2 | user_uploads no versioning | missed | **caught by absence** | No backup_retention_configured evidence for user_uploads while every other bucket has it; Gap Agent sees the absence contrast |
| 7 | readonly_auditor no MFA | missed (unparseable) | **caught** | plan JSON has the resolved policy string; existing static-JSON check just works |
| 11 | data_ops partial MFA | missed (unparseable) | partial | detector reports `mfa_required=present` because one statement has MFA; ground truth wants "mixed MFA across statements" which would require a detector enhancement |

**Updated hit-rate (plan-mode, post Phase 6-lite + coverage follow-up):**
- Full catch: 8/12 (#1, #3, #4, #6, #7, #8, #9, plus the bonus resolution of #11's data_ops partial case)
- Partial or presence-via-absence: 10/12 (#2, #5, #12 counted in as partials)
- Still missed: #10 (no `aws.kms_key_policy` detector), the data-events half of #12

Up from 5/12 full / 7/12 partial under HCL mode. The headline discovery:
**the `jsonencode(data.aws_iam_policy_document…)` detector limitation
collapses for free under plan mode** because Terraform resolves the
expression at plan time, turning the policy body into a static JSON
string that the existing detector handles. No detector code change needed.
That's the single biggest argument for plan-mode-first as the default CI
configuration.

**What plan mode still doesn't unlock:**

- **`aws.kms_key_policy` (gap #10)** — no detector exists yet; plan mode
  exposes the resolved policy JSON but we have no detector reading it.
  P2 follow-up.
- **CloudTrail data-event selectors (#12 half)** — existing detector
  doesn't check for data-event selector presence on `event_selector`
  blocks. P2 detector enhancement.
- **`data_ops` mixed-MFA case (#11 partial)** — existing detector
  emits `mfa_required=present` if ANY statement has an MFA condition.
  Real FedRAMP expectation is MFA on every privileged statement. P2
  detector enhancement: add a `mfa_required=partial` state for the
  some-but-not-all case.
- **ALB HTTP-to-HTTPS redirect (#5)** — detector sees the HTTP
  listener but doesn't check whether its `default_action` is a
  redirect vs a fixed-response. Same P2 as before.

**Operational note from the plan-generation step:** govnotes' storage
module embeds KMS key ARNs in the `buckets` map, which are
`(known after apply)` until KMS keys exist. `terraform plan -refresh=false`
fails with "for_each argument derived from apply-time results." Worked
around for this dogfood by replacing the KMS ARN references with
literal strings in a scratch copy — a real user in a FedRAMP CI would
have already applied the KMS key resources (or would `-target` them
first) so this isn't a plan-mode flaw but a "first-plan-after-init"
reality. Worth documenting in the CI-first usage section.

### Plan-mode agent behavior

Full agent pipeline was re-run against the plan-mode scan. Summary:

**Gap Agent plan-mode classification (60 KSIs):**
- Distribution: 54 not_implemented, 5 partial, 1 implemented.
- KSI-IAM-MFA rationale went from HCL-mode's "policy attribute is not
  a literal JSON string (unparseable)" for every policy → plan mode's
  specific-by-policy call: "MFA enforced on data_ops and
  platform_admin; absent on ci_deploy and readonly_auditor; three
  policies (app_task, ecs_task_execution_extras, flow_logs) remain
  unparseable." That's actionable guidance vs. "sorry, can't see."
- The Gap-Agent evidence-differentiates check still passes (3 distinct
  statuses observed: implemented, partial, not_implemented).

**Documentation Agent plan-mode narratives:**
- KSI-SVC-VRI narrative: 1482 chars, cites the 2 FIPS-listener sha256s
  with source paths — unchanged quality vs. HCL mode because the
  underlying evidence is unchanged (FIPS detector claims KSI-SVC-VRI
  directly, not via the ksis=[] encryption detectors).
- KSI-IAM-MFA narrative: 2289 chars, cites 8 evidence records by
  sha256, names specific policies and their MFA status — much richer
  than HCL mode's "all policies unparseable" rationale.

**Remediation Agent plan-mode behavior: found a bug, fixed it.**

First run (commit `8774727`) refused: `no_terraform_fix`, with prose
explaining "the only file cited by the evidence is govnotes.plan.json,
which is a Terraform plan output — a generated artifact, not a source
file that should be edited directly." Root cause: in plan mode,
root-module resources' `source_ref.file` points at the plan JSON
because plan JSON doesn't carry per-resource file info (only module-
call `source` hints). The Remediation CLI read the plan JSON as
"source" and the agent correctly refused to patch it.

Fix (next commit): CLI skips non-`.tf` source files, and when evidence
produces zero loadable `.tf` content, sweeps `target_root.rglob("*.tf")`
as a fallback. This relies on the real-world CI pattern where users
run `efterlev scan --plan plan.json --target .` from the repo root
that *also* contains the .tf files. Retry with the fix: clean unified
diff targeting `infra/loadbalancer.tf:72` upgrading `legacy_api`'s
TLS policy. Works as designed.

**Why Remediation didn't pick `user_uploads` encryption (the
DELIBERATE_GAPS.md showcase):** with plan mode, the user_uploads
encryption gap IS now in the evidence set — but from
`aws.encryption_s3_at_rest` which declares `ksis=[]` per the SC-28
precedent. The Remediation Agent filters evidence for KSI-SVC-VRI by
`KSI in ksis_evidenced`, which is only the FIPS SSL detector's
evidence. The encryption evidence is classified as unmapped and
doesn't reach the Remediation Agent's prompt for this KSI.

This is the "ksis=[] evidence invisibility" finding from the first
dogfood pass (finding #5), concrete this time: Remediation for
KSI-SVC-VRI correctly picks TLS-related evidence but can't see the
S3 encryption evidence that the 3PAO author tagged as the showcase
for this KSI. The fix is NOT to fudge the KSI mapping — it's to
either (a) wait for FRMR to update SC-28's KSI coverage, (b) add a
Gap-Agent-prompt-layer enhancement that walks FRMR's control→KSI
graph reverse-direction to surface ksis=[] evidence to KSIs whose
controls include SC-28-adjacent territory, or (c) add a deliberate
"detector overrides: `encryption_s3_at_rest` should also surface to
KSI-SVC-VRI because of the statement-level SC-13 adjacency" — option
(c) crosses the control-membership-is-necessary-but-not-sufficient
discipline we formalized in Phase 6-lite and should not be done.

Option (b) is the right long-term move — a feature of the Gap Agent
prompt, not a detector change. Defer to the next time prompt
work happens.

## What was NOT broken

- **Evidence-vs-Claims discipline held throughout.** Every narrative
  and rationale explicitly named its boundaries. The Doc Agent's
  KSI-SVC-VRI narrative proactively called out image/container/IaC
  signing as uncovered territory. This is the posture we promised.
- **Fence validator held on real-model output.** No fabricated
  citation sha256s in Gap Agent output against real Terraform.
- **Absolute-path leak fix (post-review fixup D) held.** FRMR
  attestation JSON contains only repo-relative paths.
- **Remediation explanation quality.** The picked diff was
  well-explained, named the operational caveat from the code
  comment, and offered a fallback policy for strict-compatibility
  concerns. Exact "DRAFT — requires human review" posture the
  product commits to.
